const ToastMessage = ({ message, type }) => {
  return (
    <div className="toast" style={{ background: color }}>
      <p>{error}</p>
      <small>All toasters toast toast</small>
    </div>
  )
}

export { ToastMessage }